final_order=[5,1,-1,3,2,4]

initial_order=[1 ,2 ,3 ,-1,  4, 5]
final_dict={}

for i in xrange(0,final_order):
	final_dict[final_order[i]]=i


freeindex=initial_order.indexof(-1)
resultarray=[o for i in range(0, len(initial_array)]
while (initial_hash not empty):
	for i in xrange(0,len(initial-order)):
		if initial-order.indexof(i)!=final_dict[final_order[i]]:
			swap(freeindex, initial_order(final_dict[final_order[i]]))
			swap(freeindex, initial_order.index(i))
			freeindex=indexof(i)
			resultarray[initial_hash[i]]=i
			initial_hash.remove(keys(i))
	
		
				
